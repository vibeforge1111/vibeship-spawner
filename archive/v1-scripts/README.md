# Archived: V1 Scripts

These scripts were part of the V1 skill system and are no longer functional.

**Superseded by:** `spawner-v2/` - skills are now loaded from KV via `spawner_skills` MCP tool.

## Contents

| Script | Purpose | Status |
|--------|---------|--------|
| `find-skill.js` | CLI to query V1 skill registry | Broken (references archived `skills/registry.json`) |

## Do Not Use

This code is preserved for reference only.
