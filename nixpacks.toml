# Nixpacks config for Railway deployment
# Builds the SvelteKit web app from the web/ subdirectory

[phases.setup]
nixPkgs = ["nodejs_20", "npm"]

[phases.install]
cmds = ["cd web && npm ci"]

[phases.build]
cmds = ["cd web && npm run build"]

[start]
cmd = "cd web && node build"
