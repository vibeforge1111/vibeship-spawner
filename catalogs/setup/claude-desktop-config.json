{
  "_comment": "Claude Desktop MCP Configuration for Marketing Team",
  "_instructions": [
    "1. Copy this file to your Claude config location:",
    "   - Mac: ~/Library/Application Support/Claude/claude_desktop_config.json",
    "   - Windows: %APPDATA%/Claude/claude_desktop_config.json",
    "2. Replace placeholder values (YOUR_...) with your actual keys",
    "3. Remove any tools you don't have API keys for",
    "4. Update the filesystem path to your marketing assets folder",
    "5. Restart Claude Desktop"
  ],

  "mcpServers": {
    "_comment_core": "=== CORE (Required) ===",

    "spawner": {
      "_description": "Vibeship Spawner - Skills, agents, and workflows",
      "command": "npx",
      "args": ["-y", "mcp-remote", "https://mcp.vibeship.co/mcp"]
    },

    "filesystem": {
      "_description": "Local file access for saving/reading marketing assets",
      "_setup": "Change the path below to your marketing folder",
      "command": "npx",
      "args": [
        "-y",
        "@anthropic-ai/mcp-filesystem",
        "YOUR_MARKETING_FOLDER_PATH"
      ]
    },

    "_comment_generation": "=== GENERATION TOOLS (Add as needed) ===",

    "fal": {
      "_description": "Fal.ai - Multi-model access (Flux, SDXL, video)",
      "_signup": "https://fal.ai → Get API key",
      "_cost": "Pay per use, ~$0.01-0.10 per generation",
      "command": "npx",
      "args": ["-y", "@anthropic-ai/mcp-fetch"],
      "env": {
        "FAL_KEY": "YOUR_FAL_API_KEY"
      }
    },

    "replicate": {
      "_description": "Replicate - Access to 1000s of AI models",
      "_signup": "https://replicate.com → Get API token",
      "_cost": "Pay per use, varies by model",
      "command": "npx",
      "args": ["-y", "@anthropic-ai/mcp-fetch"],
      "env": {
        "REPLICATE_API_TOKEN": "YOUR_REPLICATE_TOKEN"
      }
    },

    "elevenlabs": {
      "_description": "ElevenLabs - Voice synthesis and SFX",
      "_signup": "https://elevenlabs.io → Get API key",
      "_cost": "$5-22/month for reasonable usage",
      "command": "npx",
      "args": ["-y", "@anthropic-ai/mcp-fetch"],
      "env": {
        "ELEVENLABS_API_KEY": "YOUR_ELEVENLABS_KEY"
      }
    },

    "_comment_notes": "=== NOTES ===",
    "_note1": "Remove the _comment, _description, _signup, _cost, and _setup fields before using",
    "_note2": "Only include servers you have API keys for",
    "_note3": "The fetch MCP is used as a bridge until dedicated MCPs are built"
  }
}
