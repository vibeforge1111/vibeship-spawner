# Collaboration - Account Abstraction
version: 1.0.0
skill_id: account-abstraction

prerequisites:
  required:
    - skill: smart-contract-engineer
      reason: "ERC-4337 requires deep Solidity knowledge"
    - skill: wallet-integration
      reason: "Wallet UX and SDK integration"

delegation_triggers:
  - condition: "Building wallet frontend"
    delegate_to: wallet-integration
    context: "Provide bundler and paymaster configs"

handoff_data:
  provides:
    - account_factory_address
    - paymaster_address
    - bundler_endpoint
